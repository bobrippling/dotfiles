pcm.plugequal{
  type equal
  slave.pcm "plug:dmix"
}

ctl.equal{
  type equal;
}

pcm.equal{
  type plug;
  slave.pcm "plug:dmix";
}

#pcm.!default front:Intel

pcm.dmixout {
  # Just pass this on to the system dmix
  type plug
  slave {
     pcm "dmix"
  }
}

pcm.!default {
  type plug;
	slave { pcm "dmix" }
}
