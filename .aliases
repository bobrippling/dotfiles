alias grep='grep --color=auto'
alias ls='ls --color=auto --group-directories-first -F'
alias l='ls -1'
alias gdb='gdb -q' # quiet
alias gdb_graphical='gdb -tui'
alias valgrind-full='valgrind --leak-check=full --show-reachable=yes'
alias pstree='ps auxwwwf'
alias bc='bc -q'
alias jerbs='jobs'
# file size:
alias dub='du -b'
alias shreduvz='shred -uvz'
alias netstat='netstat 2> /dev/null'

# point to non-existant file, to ignore GTK theme
alias codeblocks='GTK2_RC_FILES=/tim codeblocks'
alias soffice='GTK2_RC_FILES=/tim soffice'
alias xselo='xsel -o|(cat;echo)'
alias gccW='gcc -Wall -Wextra -pedantic'

addtopath(){
	if [ $# -eq 0 ]
	then
		echo "addtopath() - need an argument"
	else
		echo $PATH | grep $@ &> /dev/null
		if [ $? -ne 0 ]
		then
			export PATH=$PATH:$@
		fi
	fi
}

nohist(){
	unset HISTFILE
	sed -i '/nohist/d' ~/.zsh_history
	if [ $# -ne 0 ]
	then echo 'nohist: ignored args'
	fi
}

search(){
  if [ $# -ne 1 -o "$1" = "--help" ]
  then
    echo "usage: $0 file_to_search_for" >&2
    return 1
  else
    echo "find . -iname \*$1\*" >&2
    find . -iname \*$1\*
  fi
}

searchgrep(){
  echo 'grep -Rin "$@" *'
  grep -Rin "$@" *
}

nh(){
  if [ $# -eq 0 ]
  then
    echo "usage: nh program [args]"
  else
    nohup $@ &> /dev/null &
    disown %nohup
  fi
}

car()  { echo $1; }
cdr()  { shift; echo $@; }
ucar() { echo ${!#}; }
ucdr() { echo "$*" | sed 's/ [^ ]*$//'; }

addtopath "/usr/local/bin"
addtopath "/home/rob/sh"
addtopath "/home/rob/bin"

# vim: ft=sh
